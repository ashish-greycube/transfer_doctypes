# -*- coding: utf-8 -*-
from setuptools import setup, find_packages

with open('requirements.txt') as f:
	install_requires = f.read().strip().split('\n')

# get version from __version__ variable in transfer_doctypes/__init__.py
from transfer_doctypes import __version__ as version

setup(
	name='transfer_doctypes',
	version=version,
	description='App to help transfer doctypes and related documents',
	author='GreyCube Technologies',
	author_email='admin@greycube.in',
	packages=find_packages(),
	zip_safe=False,
	include_package_data=True,
	install_requires=install_requires
)
